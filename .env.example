# Optional local settings only. The connected chat supplies reasoning.
# No AI provider API key, model name, or separate API billing is used.
USABILITY_ARTIFACT_DIR=.usability
USABILITY_HEADLESS=true
